trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  imageName: 'tasktracker'
  # Aapki registry ka naam 'rigistry' hai
  acrName: 'rigistry' 

stages:
- stage: Build
  displayName: 'Build and Push to ACR'
  jobs:
  - job: BuildPush
    steps:
    - task: Docker@2
      displayName: 'Build and Push Image'
      inputs:
        containerRegistry: 'acr-connection' #
        repository: $(imageName)
        command: 'buildAndPush'
        Dockerfile: '**/Dockerfile'
        tags: |
          $(Build.BuildId)
          latest

- stage: DeployAppService
  displayName: 'Deploy to Azure Web App'
  dependsOn: Build
  jobs:
  - deployment: WebDeploy
    environment: 'production'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureWebAppContainer@1
            displayName: 'Azure Web App on Container Deploy'
            inputs:
              # Connection name should match 'azure-connection'
              azureSubscription: 'azure-connection' 
              appName: 'tasktracker-web'
              imageName: '$(acrName).azurecr.io/$(imageName):$(Build.BuildId)'
